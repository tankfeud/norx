# Package
version       = "0.1.0"
author        = "Göran Krampe"
description   = "Minimal sample with its own main loop for ORX wrapper"
license       = "MIT"
installDirs   = @["data"]
bin           = @["sample2"]

# Dependencies
requires "nim >= 2.0.10"
requires "norx >= 0.7.0"
